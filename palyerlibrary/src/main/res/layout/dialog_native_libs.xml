<?xml version="1.0" encoding="utf-8"?>
<!-- 扩展包管理：按需下载的 native 组件与播放内核。
     每行紧凑排布（左侧名称+状态，右侧小按钮），按「功能组件 / 播放内核」
     分两张卡片，8 行一屏内可览，避免纵向长列表要滚两屏。
     与 subtitle_dialog 同为右侧滑出面板，样式保持一致。 -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/op_scrim">

    <!-- content_layout：DialogUtils 据此施加右侧滑入动效（并消费点击） -->
    <ScrollView
        android:id="@+id/content_layout"
        android:layout_width="@dimen/op_panel_width"
        android:layout_height="match_parent"
        android:layout_gravity="end"
        android:background="@drawable/setup_panel_bg"
        android:clickable="true"
        android:focusable="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="扩展包管理"
                android:textColor="@color/op_text_primary"
                android:textSize="18sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/tv_native_libs_hint"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="以下组件不再随安装包分发，用到时按需下载。"
                android:textColor="@color/op_text_secondary"
                android:textSize="11sp"
                android:layout_marginTop="4dp"
                android:layout_marginBottom="12dp" />

            <!-- ========== 功能组件 ========== -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="功能组件"
                android:textColor="@color/op_text_primary"
                android:textSize="13sp"
                android:textStyle="bold"
                android:layout_marginBottom="6dp" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:background="@drawable/setup_card_bg"
                android:paddingTop="4dp"
                android:paddingBottom="4dp"
                android:paddingStart="12dp"
                android:paddingEnd="12dp">

                <!-- 种子播放 -->
                <LinearLayout style="@style/BundleRow">
                    <LinearLayout style="@style/BundleRowText">
                        <TextView style="@style/BundleRowName" android:text="种子播放" />
                        <TextView android:id="@+id/tv_bundle_torrent_status" style="@style/BundleRowStatus" />
                    </LinearLayout>
                    <TextView android:id="@+id/btn_bundle_torrent" style="@style/BundleRowButton" />
                </LinearLayout>

                <View style="@style/BundleRowDivider" />

                <!-- 语音识别 -->
                <LinearLayout style="@style/BundleRow">
                    <LinearLayout style="@style/BundleRowText">
                        <TextView style="@style/BundleRowName" android:text="语音识别" />
                        <TextView android:id="@+id/tv_bundle_asr_status" style="@style/BundleRowStatus" />
                    </LinearLayout>
                    <TextView android:id="@+id/btn_bundle_asr" style="@style/BundleRowButton" />
                </LinearLayout>

                <View style="@style/BundleRowDivider" />

                <!-- 文字识别 -->
                <LinearLayout style="@style/BundleRow">
                    <LinearLayout style="@style/BundleRowText">
                        <TextView style="@style/BundleRowName" android:text="文字识别" />
                        <TextView android:id="@+id/tv_bundle_ocr_status" style="@style/BundleRowStatus" />
                    </LinearLayout>
                    <TextView android:id="@+id/btn_bundle_ocr" style="@style/BundleRowButton" />
                </LinearLayout>

                <View style="@style/BundleRowDivider" />

                <!-- 翻译 -->
                <LinearLayout style="@style/BundleRow">
                    <LinearLayout style="@style/BundleRowText">
                        <TextView style="@style/BundleRowName" android:text="翻译" />
                        <TextView android:id="@+id/tv_bundle_translate_status" style="@style/BundleRowStatus" />
                    </LinearLayout>
                    <TextView android:id="@+id/btn_bundle_translate" style="@style/BundleRowButton" />
                </LinearLayout>
            </LinearLayout>

            <!-- ========== 播放内核 ========== -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="播放内核"
                android:textColor="@color/op_text_primary"
                android:textSize="13sp"
                android:textStyle="bold"
                android:layout_marginTop="14dp"
                android:layout_marginBottom="6dp" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="兼容性不同，下载后在「播放内核」中切换"
                android:textColor="@color/op_text_secondary"
                android:textSize="11sp"
                android:layout_marginBottom="6dp" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:background="@drawable/setup_card_bg"
                android:paddingTop="4dp"
                android:paddingBottom="4dp"
                android:paddingStart="12dp"
                android:paddingEnd="12dp">

                <!-- IJK -->
                <LinearLayout style="@style/BundleRow">
                    <LinearLayout style="@style/BundleRowText">
                        <TextView style="@style/BundleRowName" android:text="IJK 内核" />
                        <TextView android:id="@+id/tv_bundle_ijk_status" style="@style/BundleRowStatus" />
                    </LinearLayout>
                    <TextView android:id="@+id/btn_bundle_ijk" style="@style/BundleRowButton" />
                </LinearLayout>

                <View style="@style/BundleRowDivider" />

                <!-- 阿里云 -->
                <LinearLayout style="@style/BundleRow">
                    <LinearLayout style="@style/BundleRowText">
                        <TextView style="@style/BundleRowName" android:text="阿里云内核" />
                        <TextView android:id="@+id/tv_bundle_ali_status" style="@style/BundleRowStatus" />
                    </LinearLayout>
                    <TextView android:id="@+id/btn_bundle_ali" style="@style/BundleRowButton" />
                </LinearLayout>

                <View style="@style/BundleRowDivider" />

                <!-- MPV -->
                <LinearLayout style="@style/BundleRow">
                    <LinearLayout style="@style/BundleRowText">
                        <TextView style="@style/BundleRowName" android:text="MPV 内核" />
                        <TextView android:id="@+id/tv_bundle_mpv_status" style="@style/BundleRowStatus" />
                    </LinearLayout>
                    <TextView android:id="@+id/btn_bundle_mpv" style="@style/BundleRowButton" />
                </LinearLayout>

                <View style="@style/BundleRowDivider" />

                <!-- FFmpeg 合并 -->
                <LinearLayout style="@style/BundleRow">
                    <LinearLayout style="@style/BundleRowText">
                        <TextView style="@style/BundleRowName" android:text="FFmpeg 合并" />
                        <TextView android:id="@+id/tv_bundle_ffmpeg_status" style="@style/BundleRowStatus" />
                    </LinearLayout>
                    <TextView android:id="@+id/btn_bundle_ffmpeg" style="@style/BundleRowButton" />
                </LinearLayout>
            </LinearLayout>

        </LinearLayout>
    </ScrollView>
</FrameLayout>
